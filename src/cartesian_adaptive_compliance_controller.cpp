// Empty

/**
 * Changes are in the functions:
 * - on_configure(): this changes might not be relevant to me
 * - on_activate(): there are changes
 *      mainly initialisation of publisher/subscribers (for both logging and control purposes)
 * - update(): this must be changed for sure, since it implements the main control algorithm
 *      see where we can call the parent class!
 *
 */
